
var damiao = {
  /**
   * 将数组按size一组分成若干组
   *
   * @param {Array} array 将被分组的原数组
   * @param {number} size 将被分组的原数组
   * @returns {Array} 分好的数组
   *
   */
  chunk: function (ary, size = 1) { },
  /**
   *
   */
  compact: function (array, [values]) {

  }
}
